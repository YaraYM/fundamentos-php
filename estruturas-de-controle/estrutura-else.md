# Estrutura else

- A estrutura else pode executar um outro bloco de código, o que acontece quando a expressão de if é falsa.
- Em else, não inserimos expressões.
- Exemplo:
            if (exp) {
              bloco 1
            } else {
              bloco 2
            }