# Removendo elementos

- Podemos remover elementos de um array com a função array_splice
- Passamos como parâmetros o array, o índice inicial e quantos elementos queremos remover
- Exemplo:
            array_splice($arr, 2, 1) => A partir do índice 2, remove 1 elemento